import React from "react";
import "./Educations.css";

type Props = {
  title: string;
  fromDate: string;
  duration: string;
  location: string;
  description: string;
};

function Education({
  title,
  fromDate,
  duration,
  location,
  description,
}: Props) {
  return;
}

export default Education;
